[pytest]
testpaths = tests
markers =
    unit: fast unit tests
    integration: tests requiring docker (unoserver, MinIO)
    performance: synthetic performance and browser/network tests outside the normal gate
    screenshot: visual regression tests with pixel diff, including HTML preview parity
    requires_unoserver: integration test that needs a live unoserver
    requires_pandoc: integration test that needs the pandoc binary
