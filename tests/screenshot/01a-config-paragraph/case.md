Параграф с обычным текстом и `inline-кодом` для проверки переопределений конфига.

Второй параграф — должно быть выравнивание по левому краю, отступ красной строки крупнее, шрифт Arial.

Inline-код должен быть в кавычках «…», шрифт Courier New, размер 12pt: `result = 42`.
